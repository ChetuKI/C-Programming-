#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n ;
    printf("Enter number : ");
    scanf("%d",&n);
    n>=1 ? printf("Natural no") : printf("not a natural number");
    return 0;
}